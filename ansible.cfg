[defaults]
host_key_checking = False
timeout = 30
gather_timeout = 300
inventory= /root/ansible_advance_hw/inventory

[ssh_connection]
# ssh_args = -o ControlPath=/var/shm/control:%h:%p:%r
ssh_args = -F /root/ansible_advance_hw/ssh.cfg
